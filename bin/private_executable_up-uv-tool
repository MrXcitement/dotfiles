#!/usr/bin/env bash
# up-uv-tool -- upgrade 'uv' installed tools

if [[ $(command -v uv) ]]; then
    echo "Upgrade 'uv' installed tools..."
    echo "-------------------------------"
    uv tool upgrade --all
fi

