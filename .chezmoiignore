LICENSE
README.md
.vscode/
.DS_Store

{{ if not (lookPath "bash") }}
.bash_profile
.bashrc
.bashrc.d/*
{{ end }}

{{ if not (lookPath "git") }}
.gitconfig
{{ end }}

{{ if not (lookPath "ssh") }}
.ssh/config
{{ end }}

{{ if not (lookPath "vim") }}
.vim*
{{ end }}

{{ if not (lookPath "zsh") }}
.z*
{{ end }}

# Windows folders to be ignored on other systems
{{- if ne .chezmoi.os "windows" }}
AppData
{{- end }}
